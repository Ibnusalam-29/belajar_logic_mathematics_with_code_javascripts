//cek rentang angka di rentang tertentu

let angka = 1;
let min = 10;
let max = 20;

if (angka >= min && angka <= max) {
    console.log(angka + " berada di dalam rentang " + min + " hingga " + max);
} else {
    console.log(angka + " berada di luar rentang " + min + " hingga " + max);
}